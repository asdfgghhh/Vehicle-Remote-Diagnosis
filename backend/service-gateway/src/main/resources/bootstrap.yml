spring:
  application:
    name: service-gateway
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:124.221.104.56:8848}
        namespace: ${NACOS_NAMESPACE:}
        group: ${NACOS_GROUP:DEFAULT_GROUP}
      config:
        enabled: false

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
